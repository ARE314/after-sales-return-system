> ⚠️ **历史设计稿**：本文写于 2026-09-22 数据库改 MySQL 之前，
> 文中的 `xxx.db` 即今天的 `xxx_db`（schema），数据模型、关系与字段定义均未变。
> 当前实现与部署方式请以 `README.md` / `deploy/README.md` 为准。

# 售后返件登记系统 · 框架大纲（现状 + 发货管理设计）

> 依据：2026-09-21 对仓库代码与运行数据的**实测**整理（非设计稿）。
> **全文分两部分**：第一～四章、第六～十四章为**现状实测**；**第五章「发货管理」为新增设计** ——
> 文中凡标 **规划** 的内容均**尚未实现**。
> **修订**：2026-09-21 金山文档同步模块已整体移除（数据库字段与配置一并清理）—— 见第十四章。
> 详细说明见 `README.md`；本文只讲**结构与边界**，供快速了解与后续规划用。

---

## 一、系统定位

面向风电机组售后返件的**三段式登记系统**：**退回登记 → 检测登记 → 处理登记**，
配套查询、看板与匹配数据库。单机部署（FastAPI + SQLite + 纯静态前端），两个独立入口：

- **面向人**：办公电脑浏览器（端口 8000）—— 登记、查询、看板
- **面向机器**：数据开放接口（端口 8100）—— 供外部系统定时拉取（谁来拉、拉到哪由对端决定）

> **规划**：新增「发货管理」主模块（发货申请 / 待发货清单 / 发货跟踪 / 核销台账），
> 与售后返件形成"**发货 ↔ 返件**"闭环 —— 详见第五章。

## 二、总体架构（四层）

```
使用端      办公电脑（浏览器 + 扫码枪）          外部系统定时任务
                        │                            │
服务层      app.py :8000（55 个接口）      open_api.py :8100（5 个接口）
                        └──────────┬─────────────────┘
核心层      core/ 业务核心（约 4,714 行）
            登记仓库 / 鉴权 / 照片 / 数据接口 / 连接与建表 …
                        │
数据层      returns.db · inspect.db · handle.db · delivery.db · items.db · auth.db
            另有：data/photos 照片 · exports 导出 · backups 备份
```

> 发货管理新增的 `delivery.db` 同样遵循「一个模块一个库」，上述**四层结构不变**。

## 三、运行形态

| 项 | 现状 |
|---|---|
| 技术栈 | Python + FastAPI / Uvicorn；前端原生 JS + Chart.js（无框架） |
| 端口 | **8000** 界面与业务接口；**8100** 数据开放接口（可单独放行/关闭） |
| 网络 | 默认 `127.0.0.1` 仅本机；改 `config.HOST` 开放局域网 |
| 数据 | 5 个本地 SQLite 库 + 照片 / 导出 / 备份目录（全本地，不出网） |
| 第三方依赖 | 仅 4 个：fastapi · uvicorn · openpyxl · Pillow |
| 启动 | `python app.py` 或 `start.bat` |
| 鉴权 | 默认开启（可运行时关）；权限组 + 权限点；会话 12 小时 |

## 四、功能模块（两个主模块 → 子模块）

按业务方向分**两个主模块**，外加一组支撑页面：

| 主模块 | 方向 | 子模块 | 状态 |
|---|---|---|---|
| **售后返件** | 收回来 | 退回登记 · 检测登记 · 处理登记 · 明细查询 | 已实现 |
| **发货管理** | 发出去 | 发货申请 · 待发货清单 · 发货跟踪 · 核销台账 | **已实现**（四件套全部落地 · 见第五章） |
| 支撑与公共 | — | 工作台 · 数据看板 · 匹配数据库 · 数据接口 · 权限设置 · 登录 | 已实现 |

**两个主模块的边界**：返件管"**一件产品进来后走完流程**"；发货管"**一批货出去后有没有回来**"。
两者只在**核销台账**处交汇（发货数量 ↔ 返回件核销），其余环节互不侵入。

**现状页面清单（14 个，已实现）**

| # | 页面 | 文件 | 职责 | 权限点 |
|---|---|---|---|---|
| 1 | 工作台 | index.html | KPI、最近登记、系统信息、快捷入口 | page.index |
| 2 | 退回登记 | scan.html | 自动匹配栏 + 整单信息 + 多行明细（扫码枪） | page.scan |
| 3 | 检测登记 | inspect.html | 待检清单 + 检测结果与归因录入 | page.inspect |
| 4 | 处理登记 | handle.html | ERP 处理与后续方案 | page.handle |
| 5 | 明细查询 | query.html | **一级窗口 = 售后单列表（一单一行、12 列含整机厂家/项目风场/快递单号，自带分页）+ 点单弹窗看该单明细**（2026-09-23 方案 B：已去掉「全部明细 / 列设置 / 批量删除」入口，多值厂家/风场全部列出）；全字段检索 / 编辑 / 照片 / 导出 | page.query |
| 6 | 数据看板 | dashboard.html | 返件汇总 / 检测汇总 两视角图表 | page.dashboard |
| 7 | 匹配数据库 | items.html | 物料主档（1,735 条）维护与导入 | page.items |
| 8 | 发货申请 | delivery-apply.html | 申请单列表 + 新建/编辑/详情；主表 8 项 + 产品明细行（**自物料库模糊搜索选定，不支持手填**）；不做审批 | page.delivery_apply |
| 9 | 待发货清单 | delivery-pending.html | 发货员的清点清单（**派生自申请单，不落表**、**只读不写**）；按期望发货日升序、超期标红；逐行显示「已发 x / y 行」进度，每行「去登记发货」跳 ③ | page.delivery_pending |
| 10 | 发货跟踪 | delivery-track.html | **以申请明细为主体**：申请单一提交即出现（待发货）；按行登记发货单号（**必填**）、整单登记完状态自动转已发货；Excel 批量导入（**幂等**）、按料号人工挂接 | page.delivery_track |
| 11 | 核销台账 | delivery-ledger.html | 发货 × 返件核销：厂家 + 风场 + 型号维度上的「剩余未返回」；手工清账（**原因必填**） | page.delivery_ledger |
| 12 | 回收站 | recycle.html | **删除不再一去不回**：明细 / 核销记录 / 匹配库物料先落快照再算删掉，保留 7 天可还原（连主键写回）、可彻底删除；过期由后台任务清理 | page.recycle |
| 13 | 数据接口 | api.html | 对外接口：令牌 / IP 白名单 / 数据集 | page.api |
| 14 | 权限设置 | auth.html | 用户 / 权限组 / 会话 / 日志；权限组页附**权限一表全览**（行=权限点、列=权限组，一眼看出哪个组缺哪项） | page.auth |
| 15 | 登录 | login.html | 登录页（不设权限点） | — |

## 五、发货管理（新增主模块 · 设计稿）

**本章状态：四件套全部实现（2026-09-21）** ——
`delivery-apply.html` / `delivery-pending.html` / `delivery-track.html` / `delivery-ledger.html`
+ `core/repo_delivery.py` + `delivery.db`（`delivery_request` / `delivery_request_item`
/ `delivery_shipment` / `ledger_clear`）。**唯一未做的**是 ③ 与 ERP 的实际对接
（表与接口位已就绪，届时由同步任务写 `delivery_shipment` 的 `source='erp'`）。
> 落地时**必须遵循既有约定**：一个模块一个库 · 写入不跨库 · 跨库 LEFT JOIN + COALESCE ·
> 派生表可重建 · 人工优先（自动逻辑不得改写人工结果）。

### 5.1 主模块定位

与「售后返件」互为**反向**的一条业务线：返件是**收回来的产品**，发货是**发出去的产品**。
本模块只回答三个问题 —— ① **该发什么**（申请与待发货）② **发了什么**（跟踪）
③ **还差多少没回来**（台账）。

主线是一条**单据链**：`申请 → 待发货 → 发货 → 台账`，**每一环都由上一环派生，不重复录入**。

### 5.2 四个子模块

| 子模块 | 定位（回答什么） | 数据来源 | 与相邻子模块的关系 |
|---|---|---|---|
| **① 发货申请** | 需求入口：发什么、发到哪、**是否需要返回** | **本系统内录入**（标准表单，不依赖外部系统） | 唯一数据起点：**提交即生成待发货清单**（不做审批；将来接入审批时由预留钩子接管） |
| **② 待发货清单** | 清点清单（**只读**）：**已提交未发完**的申请，给发货员照着去 ERP 做单 | **派生自 ①**（不重复录入、**不写任何数据**） | 上游=①；**不与其他模块产生数据关系** —— 全部明细在 ③ 登记完，它自然不再列出该单 |
| **③ 发货跟踪** | **以申请明细为主体**：申请了的货**发出去了没有** | **申请明细（①）∪ 发货记录**；发货记录按行登记或 Excel 导入，将来由**预置端口接 ERP** | 上游=①（**提交即出现**，不等 ② 的任何动作）；已登记的行 → ④ 的借方 |
| **④ 核销台账** | 总账：**发了多少 / 回来多少 / 还差多少** | **发货清单（借方）+ 返回件登记（贷方）**，按「整机厂家 + 项目风场」匹配 | 上游=③ + 退回登记（跨主模块）；输出剩余未返回与清账 |

#### ① 发货申请

- **字段定义已到 v0.5（2026-09-21）** —— 详见 `发货申请单-字段定义.md`，
  原型见 `发货申请单-初版.html`（可直接试填、试校验）。要点：

  | 类别 | 内容 |
  |---|---|
  | **主表（整单信息 · 8 项）** | 风机厂家 · 项目名称 · **期望发货日** · 收件地址（**可整段粘贴「地址+姓名+电话」自动拆分**）· 联系人 · 电话 · 调换原因（**整单一个**）· 快递要求 |
  | **明细行（发货内容 · 一行一种产品）** | **产品**（**主输入**：输入 料号 / 品名 / 规格 / 型号 关键词 → 从候选里选定）· **数量** · **是否需要返回（逐行）** · 料号 · **产品型号** · 规格（**选定后自动带出，只读**） |
  | 系统字段（5 项） | 申请单号 `request_no` · 申请人 `applicant` · 申请日期 `apply_date` · 状态 `status` · 行号 `line_no` |
  | **不建** | 审批人 / 审批时间 / 审批意见 —— 系统不做审批，仅预留接口 |

- **⚠️ 明细行的主输入不是「型号」，是「搜索框」（v0.5 定案，实现时最容易想错的地方）**：
  实测物料库 **料号 100% 唯一、型号不唯一**（同一型号常对应几十上百个料号 ——
  `BLF1-S` 100+ 条、`BLF1-C` 114 条，差别只在规格 / 描述里）。
  所以正确方向是 **「按 料号/品名/规格/型号 模糊搜 → 选候选 → 型号随之得出」**，
  而不是「先选型号 → 再挑哪个料号」（后者用户没有判断依据）。
  **产品必须从候选中选定（v0.6，不支持手工填写）**：手填给不出料号 / 型号，
  台账核销与退回登记对齐都会失去依据 —— 搜不到就等于这行填不了，需先补物料库。
  详见 `发货申请单-字段定义.md` 2.2~2.5。
- **字段名与退回登记对齐**：`turbine_vendor` / `project_site` 同名同义，这是台账按
  「整机厂家 + 项目风场」核销的前提。与最初设计稿相比：**数量已加入**（在明细行）、
  备注未加，料号 / 型号 / 规格改为**由搜索结果带出的只读列** —— 以字段定义文档为准。
- **⚠️ 核销粒度差异（实现台账时最容易想错的地方）**：退回登记**一行 = 一件产品**
  （`detail_key` 一件一个键）；发货明细**一行 = 一种产品 + 数量**（可能 N 件）。
  所以台账核销是 **「1 行发货 ↔ N 条返件」**，必须按**数量**匹配，不能靠行号对应。
- **「是否需要返回」是分流开关，且挂在「明细行」上（逐行判断）**：**默认勾选**
  （调换件通常要还旧件）。勾选的行发货后进入台账等着与返件核销；取消勾选的行
  （一次性耗材等）只走发货流程，**不产生未返回数**。放明细行是因为一单可能混装 ——
  例如 3 个接近开关（要返回旧件）+ 2 卷胶带（不返回），放主表就表达不了。
- **流转（无审批）**：`草稿 → 已提交（进待发货清单）→ 已发货 → 已关闭`。
- **衔接**：提交**即进入 ② 待发货清单**，不需要再次录入；后续环节（发货、核销）都在
  ② ③ ④ 中发生，① 不再变更。
- **审批预留（不实现）**：状态机预留 `pending_approval` / `rejected` 两个取值（当前不可达），
  并预留 `on_submit` / `on_approve` / `on_reject` 三个钩子。将来接入只需三处改动，
  **下游（②③④）不必改** —— 它们只认「`status == 'submitted'`」。
  详见字段定义文档第四节。

#### ② 待发货清单　✅ 已实现（2026-09-21 · 当日改版为**只读清点视图**）

- **本质是申请表的派生视图**：筛 `status = 'submitted'` 的**整单**，**不单独落表**。
- **⚠️ 2026-09-21 改版：本页变成只读，一个字节都不写。**
  改版前它是「执行队列」—— 仓库在这儿点「标记已发货」出队，发货记录**要等这个动作**
  才产生，于是 ③ 在时间上永远滞后（用户指出：申请单一提交就该在跟踪表里看到）。
  改版后发货记录只在 **③ 按行登记**时产生；本页**不再有任何写动作** ——
  整单出队 / 撤销出队 / 手工登记三个接口已删除，勾选框与批量条一并去掉。
- **它现在的定位是「发货员的清点清单」**：发货员照着一行一行去 ERP 做发货单，
  做完到 ③ 登记。所以每行给的是「去登记发货」**链接**（带 `request_no` 跳到 ③，
  跳过去自动筛出这张单的全部明细），而不是一个「标记一下」的按钮。
- **每行显示「已发 x / y 行」进度**（`shipped_lines` / `apply_lines`）：
  **部分发货的单仍留在清单里** —— 它确实还有没做的。汇总另给「单已部分发货」，
  那是发货员最需要回头盯的一批（一张单分几次做很常见）。
- **默认按期望发货日升序**，并把「已超期的单」标红（`超期 N 天`）——
  清单要回答的是「接下来该发哪些、谁最急」，这跟按提交时间倒序的申请列表
  是**两种不同的排序语义**，不要混。
- **整单什么时候离开清单**：该申请单的**全部明细行都登记了发货**（状态自动转
  `shipped`）。这个状态是**推论**，不是人去点的 —— 见 ③。

#### ③ 发货跟踪　✅ 已实现（2026-09-21 · 当日改版为**申请明细为主体**）

- **⚠️ 改版要点：列表主体是「申请明细」，不是「发货记录」。**
  申请单一**提交就出现在这里**（状态「待发货」，还没有发货单号）——
  因为「发货跟踪」跟的是**申请了的货发出去了没有**；只列发货记录的话，
  它只能回答"已经发生了什么"，回答不了"还差哪些"。
  列表 = **申请明细（主体）∪ 对不上明细的发货记录（未关联）**，用**应用层合并**
  （申请了没发的、发了没申请的都可能只在一边出现，SQLite 没有 FULL OUTER JOIN，
  用 LEFT JOIN 串一定漏 —— 同 ④ 的做法）。
- **三种行态**（`config.DELIVERY_TRACK_STATES`）：
  | 行态 | 含义 | 可用动作 |
  |---|---|---|
  | `pending` 待发货 | 申请已提交，还没登记发货 | 登记发货 |
  | `shipped` 已发货 | 已登记，带着 ERP 单号 | 修改 / 撤销登记 |
  | `unlinked` 未关联 | 有发货记录但对不上任何申请明细 | 挂接 / 删除 |
  三种动作**互不重叠** —— 待发货的行不该出现"撤销登记"，已发货的行也不该出现"挂接"。
- **登记粒度 = 申请明细行**（型号级）。台账按型号核销，一行一次登记；
  支持少发（实发件数可小于申请件数）。
- **发货单号必填** —— 台账上每一笔都要能追回 ERP 单据；它同时是导入那条记录的
  幂等键的一半「(发货单号 + 料号)」。没有单号的"已发货"在对账时查不下去。
- **一行只登记一次**：已登记的行**不覆盖**，要改走「修改」（PUT），
  要退回走「撤销登记」—— 否则误点一下就把单号换了，而对账时看不出换过。
  修改**不允许改归属**（申请单号 / 行号）：那会静默改变台账的核销维度，
  要换行必须撤销后重新登记。
- **厂家 / 风场 / 型号 / 需返回全部从申请单带出**，不接受前端传入：
  台账按前三项核销，必须与申请单完全一致，否则登记完核不上。
- **申请状态是明细登记情况的推论**（`_sync_request_status`）：
  全部明细登记完 → 自动 `shipped`；撤销登记 → 自动回落 `submitted`。
  ⚠️ 改版前「已发货」是人在 ② 点出来的**独立动作**，会出现分叉
  （甲点了标记、乙去看跟踪却没有发货单号）；现在人不需要也不应该手工改它。
- **落地表 `delivery_shipment`**：`整机厂家 / 项目风场 / 申请单号 / 行号 /
  发货单号 / 发货日期 / 物流单号 / 承运商 / 件数 / 料号 / 产品型号 / need_return /
  来源 / 备注 / 同步时间 / 操作人`。
  **厂家与风场冗余存在发货记录上、不靠 JOIN 申请单取** —— ERP 同步来的记录可能
  根本没有申请单号，靠 JOIN 会整批漏掉（那些货同样要参与核销）。
- **数据来源**：`request`（按申请单登记，主流程）/ `import`（Excel 粘贴；
  将来 ERP 同步也走这里，新增 `erp` 即可，**界面与业务逻辑不用改**）。
  ~~手工登记~~ 已于 2026-09-21 移除 —— 发货记录必须挂在申请明细的某一行上。
- **与申请行的关联**：按行登记天然带 `(申请单号, 行号)`；对不上的记录**不猜**，
  列出来让人**人工挂接**。挂接时按**料号**在该单内找明细行并补上行号，
  同时**把 `need_return` 同步成该行的值**（"要不要回收旧件"是申请单上的属性，
  不同步会让不返回的行凭空进台账 —— 实测：台账发货从 40 变 46）。
  若该行**已有**发货记录，挂接**不抢**（留空继续显示「未关联」，由人比对哪条才对）。
- **幂等**：`(发货单号 + 料号)` 相同的重复导入/同步**只更新、不新增**，
  刻意**不含申请单号**（含进去会把同一笔物理发货判成两条，台账虚增一倍 —— 实测踩过）。

#### ④ 核销台账　✅ 已实现（2026-09-21）

- **口径（一句话）**：`剩余未返回 = 发货（只算需返回的） − 已返回 − 手工清账`，
  **实时算**，不落汇总表。
- **匹配维度是三项**：`turbine_vendor`（整机厂家） + `project_site`（项目风场） +
  `product_model`（产品型号）。
  ⚠️ **原设计写的是「料号」，落地时改成了「型号」** —— 依据是数据本身：
  退回侧 `material_no` **100% 为空**（那批历史数据没填），而 `product_model` 100% 有值；
  「型号反查料号」也不可行（34 个型号里 26 个不在物料库里）。
  **型号是收、发两侧唯一的共同语言**。料号照旧在发货侧记录与展示，只是不参与匹配；
  将来退回登记开始填料号，可升级成「料号优先、型号回退」。
- **只核销 `need_return = 1` 的发货行**：一次性耗材 / 随机附件不回收，不进台账
  （否则它们会永远挂着"未返回"）。
- **默认只列有发货记录的维度**：「只有返件、从没发过货」的维度属于退回登记的事；
  混进来会让台账出现一大片「剩余 = 负数」（历史返件没有对应的发货记录），
  既盖住真正要看的东西，也让汇总的"剩余"失去意义（实测踩到）。要看全量传 `include_noship=1`。
- **返件多于发货的行会标出来**（红底 + 「返件多于发货」角标）—— 这多半意味着
  该维度的历史发货没登记，是需要补录的信号，而不是"欠了 N 件"。
- **不落核销关系表**：`ledger_clear` **只存手工清账**。自动核销的"关系"是算出来的
  （在数量级粒度下，哪一件返件对应哪一件发货本就无法唯一确定，存下来的关系没有信息量，
  只会随数据变动过期）；**手工清账是人的判断**（换件不返还 / 现场留用 / 遗失），
  算不出来，必须记下来。
- **核心指标**：**剩余未返回数量 = 需返回的发货数量 − 已自动核销数量 − 已手工清账数量**。
- **清账两种方式**：
- **自动核销**：不需要额外的"核销动作" —— 返件一登记进 `returns.db`，台账下次读取时
  就把它算进"已返回"（**只读返件、不改返件原始数据**，符合「写入不跨库」）。
  核销关系是**算出来的结果**，不是需要维护的状态。
- **手工清账**：针对换件不返还 / 现场留用 / 遗失等算不出来的差额，**原因必填**（至少 2 字），
  连同操作人与时间写入 `ledger_clear`；可撤销（撤销后差额重新回到"剩余未返回"）。
  清账不能超过当前剩余 —— 否则台账会出现负值。
- **明细可核对**：台账每一行都能点开看**发货记录 / 返件记录 / 手工清账记录**三块明细 ——
  算出来的数字必须能逐笔对上，否则没人会信它。
- **查询与导出**：按 厂家 / 风场 / 型号 / 日期范围 查「剩余未返回」，支持只看有剩余、
  导出当前视图（CSV，带 BOM，Excel 打开中文不乱码）。

### 5.3 子模块关系（单向派生，台账处汇合）

```
单据链（① 是唯一数据起点；**③ 的表体就是 ① 的明细**）
① 发货申请 ──提交──▶ ③ 发货跟踪：该单全部明细逐行出现（状态「待发货」）
   本系统录入            │
   （含「是否需要返回」）  │ 发货员照 ② 去 ERP 做发货单，回来**按行登记**（发货单号必填）
                        ▼
                  ③ 该行转「已发货」──（整单明细都登记完）──▶ 申请状态**自动**转 shipped
                        │
                        ▼
                  ④ 核销台账（只算 need_return = 1 的已登记行）

② 待发货清单（**只读**，由 ① 派生；与 ③ **不产生数据关系**）
   ├─ 发货员照它一张张去 ERP 做单（按期望发货日升序、超期标红）
   ├─ 部分发货的单仍留在清单里（显示「已发 x / y 行」）
   └─ 全部明细在 ③ 登记完 → 该单自然不再列出；本页不写任何数据

台账汇合（④ 同时吃两条线，方向相反）
  发货清单（③）───── 借方：发了多少 ──┐
                                      ├──▶ ④ 核销台账 ──▶ 剩余未返回数量
  返回件登记（退回）── 贷方：回来多少 ──┘        （自动核销 / 手工清账）
        匹配维度：整机厂家 + 项目风场
```

### 5.4 与既有模块的衔接（边界）

| 既有模块 | 衔接点 | 说明 |
|---|---|---|
| 退回登记（returns.db） | **核销台账的贷方** | 只**读**退回登记（跨库 LEFT JOIN + COALESCE）；发货模块**不写** `returns.db` |
| 整单信息字段 | **匹配维度复用** | 直接用 `turbine_vendor` / `project_site`，**不新建字典** |
| 匹配数据库（items.db） | 物料搜索 | 发货明细的**主输入**直接用 `item_master` 做模糊匹配（料号 / 品名 / 规格 / 型号 → 选定 → 带出型号），与退回登记的料号回填**同一套数据、同一方向** |
| 权限设置（auth.db） | 权限点 | 新增 9 个权限点（4 个页面 + 5 个动作，见 5.6），沿用「权限组 + 权限点」模型 |
| 数据接口（open_api） | **预置端口同源** | ERP 同步沿用「数据集 + 令牌 + 来源 IP 白名单」形态 |
| 数据看板 | **规划** | 后续可增第三视角「发货」，形态与现有两视角一致 |

### 5.5 数据落点（`delivery.db` · 规划）

| 表 | 说明 |
|---|---|
| delivery_request | 发货申请**主表**（整单信息 8 项 + 系统字段；**不含审批字段**）—— 建表片段见 `发货申请单-字段定义.md` 第八节 |
| delivery_request_item | 发货申请**明细**（**一行一种产品**：行号 / 料号 / 型号 / 品名 / 规格 / 数量 / 需返回；前三项由搜索选定后带出）—— `UNIQUE(request_no, line_no)` |
| delivery_shipment | 发货记录（**已建表**：申请单号 / 发货单号 / 日期 / 物流单号 / 承运商 / 件数 / 来源 `manual`\|`erp` / 备注 / 同步时间 / 操作人） |
| ledger_clear | 清账记录（**已建表**：维度三项 / 清账件数 / **原因（必填）** / 操作人 / 时间）—— **只存手工清账**，自动核销不落表（见 5.2 ④） |
| op_log | 操作日志（沿用既有形态） |

- **待发货清单不落表**：它是申请表的派生视图（遵循「派生表可重建」约定）。
  ⚠️ 2026-09-21 改版后它**连「出队」这个写动作也没有了** —— 只剩「读」，
  没有任何增删改接口（`POST /api/delivery/shipments`、`.../revoke`、`.../manual`
  三个接口已删除，自检里有 405 断言守着）。
- **写入不跨库**：清账只写 `delivery.db`，不碰 `returns.db`。
  **台账读取是跨库的**（发货与清账在 `delivery.db`、返件在 `returns.db`），
  但**只读不写** —— 三份数据各查一次、在应用层合并（SQLite 没有 FULL OUTER JOIN，
  用 LEFT JOIN 串会漏掉只在返件侧出现的行）。
- **人工优先**：申请状态、发货行与申请行的关联、手工清账，系统均不得自动改写人工结果。

### 5.6 权限点预留（规划）

| 权限点 | 用途 | 状态 |
|---|---|---|
| `page.delivery_apply` | 发货申请 | ✅ 已启用 |
| `page.delivery_pending` | 待发货清单 | ✅ 已启用 |
| `page.delivery_track` | 发货跟踪 | ✅ 已启用 |
| `page.delivery_ledger` | 核销台账 | ✅ 已启用 |
| `act.delivery_submit` | 提交 / 修改申请 | ✅ 已启用 |
| `act.delivery_edit` | 修改发货申请 | ✅ 已启用（登记员有） |
| `act.delivery_delete` | 删除发货申请 | ✅ 已启用（**只给管理员** —— 单子是跟着货走的凭证） |
| `act.delivery_ship` | 登记发货 / 撤销登记 | ✅ 已启用（**登记 / 修改 / 撤销 / 导入 / 挂接 / 删记录共用一个点**，都是「改发货记录」这件事的不同入口；2026-09-21 起**不再有「整单出队」**） |
| `act.delivery_approve` | 审批申请 | **预留，不启用**（系统不做审批流程） |
| `act.delivery_clear` | 清账（含手工清账） | ✅ 已启用（**默认只给管理员** —— 它会把台账上的差额抹掉，是这套流程里最需要克制的动作） |

### 5.7 落地分期（建议）

| 期 | 范围 | 依赖 |
|---|---|---|
| 一期 | ~~发货申请 + 待发货清单~~ **✅ 已完成（2026-09-21）**。主表 + 明细 + 发货记录三张表已落地；出队改为手工标记（比原计划更直接，不再需要 Excel 导入这一环） | 无（系统内闭环） |
| 二期 | ~~发货跟踪~~ **✅ 页面与表已落地**；**仅剩 ERP 实际对接**（届时同步任务写 `delivery_shipment` 的 `source='erp'`） | ERP 侧接口 + 白名单 |
| 三期 | ~~核销台账~~ **✅ 已完成**（跨库核销 / 手工清账 / 剩余未返回查询与导出）。**待办**：未返回预警（阈值提醒） | 一期数据积累 + 跨库读取 |

## 六、业务数据模型（三段式）

关联键：**detail_key（明细键）** —— 一条退回明细 = 一件产品。

**① 退回登记**（收到货就能填）
- 整单信息 **7 项**：收货信息 / 退回单号 / 快递公司 / 退回时间 / 售后单号 / 风机厂家 / 项目风场
- 明细 **11 列**：产品编号 / 料号 / 型号 / 类别 / 品名 / 规格 / 生产年 / 生产月 / 退回数量 / 反馈问题 / 分析报告

**② 检测登记**（送检后才能判定）**10 项**
- 检测信息：检测时间 / 检测结果 / 故障原因 / 改善措施 / 处理方案
- 归因与结论：问题分类 / 责任归属 / 完结状况 / 报告编号 / 照片证据

**③ 处理登记**（事后跟进）**2 项**：ERP 处理 / 后续处理方案

存储方式：**稀疏** —— 没检测的明细在检测库没有行（不是空行）。

> **规划**：发货管理侧的数据模型（申请 / 发货 / 清账）见 5.5，同样遵循「一模块一库 + 稀疏存储」。

## 七、数据库（6 库 · 一个模块一个库）

| 库 | 表（当前行数） | 职责 |
|---|---|---|
| returns.db | returns 100 · model_dict 34 · dict_option 117 · op_log 3,113 | 退回登记 |
| inspect.db | inspect_records 100 · dict_option 33 · op_log 822 | 检测登记 |
| handle.db | handle_records 100 · op_log 402 | 处理登记 |
| delivery.db | delivery_request · delivery_request_item · op_log | 发货申请（**主表 + 明细子表**，本项目第一对真正的父子表） |
| items.db | item_master 1,735 | 匹配数据库（料号 → 型号 / 品名 / 规格） |
| auth.db | users 2 · permission_group 3 · session 133 · setting 5 · op_log 917 · open_api_log 578 | 账号 / 权限 / 会话 / 接口日志 |

**两条铁律**（改代码必须遵守）：
1. **写入不跨库** —— ATTACH 无跨库原子事务，靠约定规避；
2. **跨库查询 LEFT JOIN + COALESCE 兜底** —— 否则漏掉未检测 / 未处理的行。

> 发货申请库已落地。**它与前三库有个结构性差别**：退回 / 检测 / 处理靠 `detail_key`
> 串起来（1:1 扩展），而发货申请是**主表 + 明细子表**（1:N），关联键 `request_no` ——
> 所以它**不进** `repository.py` 的 `_DETAIL_FROM` / `_qualify` 那套骨架，自成一套查询
> （见 `core/repo_delivery.py`）。将来核销台账跨库读 `returns.db` 时，两条铁律同样适用 —— 见 5.5。

## 八、后端结构

| 文件 | 行数 | 职责 |
|---|---|---|
| app.py | 1,532 | **55 个路由**：页面 / 退回·检测·处理业务接口 / 照片 / 导出 / 鉴权 / 接口配置 |
| open_api.py | 308 | 对外数据接口（独立端口）：连通 / 数据集 / 取数 / 导出 |
| config.py | 345 | 全部可调项（环境变量前缀 `ARS_`：HOST/PORT/令牌/白名单/备份阈值…） |
| core/repository.py | 2,085 | 登记侧全部 SQL（字段白名单防注入） |
| core/db.py | 683 | 连接、ATTACH 挂库、建表、迁移 |
| core/auth.py | 660 | 登录、权限组、会话、操作日志 |
| core/openapi.py | 490 | 对外接口设置与取数（三重鉴权） |
| core/photos.py | 309 | 照片存储 + 缩略图（原图保留） |
| core/repo_inspect.py | 258 | 检测侧 SQL |
| core/repo_handle.py | 228 | 处理侧 SQL |
| **合计** | **≈ 6,900** | |

## 九、前端结构

| 文件 | 行数 | 职责 |
|---|---|---|
| static/assets/common.js | 1,112 | 布局骨架、侧栏/顶栏、组合控件（下拉 / 可搜索下拉）、**全局扫码枪**、图表色板、站点英文标题常量 |
| static/assets/app.css | 1,166 | 设计令牌（对齐 GitHub Primer）+ 组件样式 |
| 各页面 HTML（10 个） | 5,689 | 每页一个文件；脚本为单个 IIFE（约定：立即执行调用统一放文件末尾，防 TDZ） |
| static/assets/lib/ | — | chart.umd.js（在用）· zxing.min.js（闲置保留） |

## 十、权限与安全

- 权限模型：**权限组 + 权限点（17 个）**
  - 界面访问 9：`page.*`（对应 9 个页面）
  - 数据操作 5：`act.create` / `act.edit` / `act.delete` / `act.export` / `act.items`
  - 系统管理 3：`act.openapi` / `act.user` / `act.settings`
- 预置组：管理员 / 登记员 / 只读（内置不可删）；可自建组逐项勾选
- 密码 PBKDF2-HMAC-SHA256；会话 = 服务端记录 + HttpOnly Cookie（退出/停用即时失效）
- 对外接口**三重鉴权**：令牌 + 来源 IP 白名单 + 数据集范围；默认只监听 `127.0.0.1`
- 只读接口（查询 / 看板）不设权限点：登录且看得见页面即可读

> **规划**：发货管理预留 **7 个权限点**（4 页面 + 3 操作），见 5.6。

## 十一、数据流向

```
写入：浏览器 / 扫码枪 → app.py:8000 → core → returns / inspect / handle.db
读取：                          ├─ 明细查询 · 数据看板 · Excel 导出
                                └─ open_api.py:8100 ── 外部系统定时拉取
发货（规划）：发货申请 → 待发货清单 → 发货跟踪(ERP) → 核销台账 ← 退回登记（按厂家+风场核销）
辅助：items.db（扫码回填依据）· photos/（照片）· backups/（备份）
```

外部协作要点：
- **快递公司识别**：`SF→顺丰` / `JD→京东` / `YT→圆通`（前缀规则），其余人工选
- **扫码枪**：全局监听键盘，不需输入框；焦点在输入框内时避让（先扫后补录）
- **对外接口方向**：由**对端**来拉本系统（本系统不持有任何外部凭据、不出网）
- **ERP 预置端口（规划）**：发货跟踪经预留接口读取 ERP 发货清单，沿用「数据集 + 令牌 + IP 白名单」形态

## 十二、工程化与质量

| 项 | 现状 |
|---|---|
| 自检 3 套 | smoke_test.py（后端 399 项）· check_frontend.js（静态 31 项）· check_ui.js（真实 DOM 269 项） |
| 工具脚本 | tools/ 15 个（备份 / 迁移 / 导入 / 对账 / 部署检查 / 密码重置…） |
| 部署 | Dockerfile + docker-compose + systemd / nginx 样例（deploy/） |
| 备份 | tools/backup.py + 过期提醒（30 小时阈值）+ 备份校验；最近一次 2026-09-20 11:41 |
| 静态资源 | 版本号 `?v=…` 机制：改 assets 必须递增（防浏览器缓存） |
| 回归流程 | 已沉淀为技能：起服务 → 三套校验 → 变异测试 → 版本号 → 写记忆 |

## 十三、规模统计

| 部分 | 行数 |
|---|---|
| 后端（app.py + open_api.py + core/ + config.py） | ≈ 6,900 |
| 前端（10 页面 + common.js + app.css） | ≈ 7,970 |
| 测试（3 套） | ≈ 4,650 |
| 工具脚本（15 个） | ≈ 2,490 |
| **合计** | **≈ 22,000 行** |

> **规划**：发货管理落地后将新增 4 个页面 + 1 个库 + 相应校验断言，
> 届时本表与第四章一并更新。

## 十四、现状观察（本次实测发现，供决策）

1. **金山文档同步模块已整体移除（2026-09-21）**：连带清掉了 `returns` 表的
   `sync_state` / `synced_at` 两列、`sync_log` 表、`config.KDOCS_TARGET` 与页面上
   的金山侧目标填写区（旧库在服务启动时自动清理）。原来工作台的「同步设置」死链
   与「待同步」指标也一并消失 —— **上述第 1 项旧问题已修复**。
2. **对外数据接口保留，但尚无外部接入**：`open_api_log` 578 条记录仍**全部来自
   127.0.0.1**（本机测试），IP 白名单为空（`open_api_ips = []`）。要真正对接
   （例如发货管理接 ERP），还需：改 `OPEN_API_HOST` + 放行端口 + 填白名单
   （令牌与数据集已配好）。该接口是通用的取数口，不绑定任何特定对端。
3. **闲置保留项**：`.scan-hero*` 等一组 CSS（原扫描区样式）当前无页面引用；
   `zxing.min.js` 文件保留但无引用 —— 均为"可能恢复"的留档，代码内有注释标记。
4. **数据量现状**：退回 100 条明细 / 131 件 · 检测 100 条 · 处理 100 条 · 物料主档 1,735 条。
5. **第五章为设计稿**：发货管理的 4 个子模块、`delivery.db`、7 个预留权限点**均未实现**，
   落地顺序见 5.7。
