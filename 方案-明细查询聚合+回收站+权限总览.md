# 方案（待审）：明细查询按售后单聚合 · 回收站 · 权限一表全览

> 状态：**待你审阅**。按“先出大纲、通过后才写入程序”的老规矩，本轮只出方案，未改任何代码。
> 硬约束（你说）：**保持现有数据结构与整体交互流程不变** —— 本方案只**新增**（新表 / 新接口 / 新页面 / 新权限点），不改现有表结构、不改现有接口签名与返回体。

---

## 一、明细查询：按售后单号聚合 + 右侧明细联动

### 现状（已核对）
- `static/query.html`（1380 行）目前是**明细级**列表：一行 = 一只产品（`detail_key` = `售后单号-行号`，见 `core/repository.py:253 make_detail_key`）。
- 数据来自 `GET /api/returns`（`app.py:811`）→ `core/repository.py:1632 query_returns`，返回 `{total, page, page_size, pages, rows}`。
- 点一行是**弹窗**编辑（`static/query.html:947 openDetail`），不是右侧面板。
- 筛选卡已有「收起/展开」按钮（`static/query.html:351-375`），但**默认展开**。
- 系统里**已有**“按售后单号聚合”的成熟写法可直接复用：`core/repository.py:1663 query_inspect_orders`（`GROUP BY r.order_no`，一行 = 一个售后单，附进度与整单状态），`core/repository.py:1746 query_handle_orders` 同款。

### 方案
**后端（只新增）**
1. `core/repository.py` 新增 `query_return_orders(filters, page, page_size, sort_by, sort_dir)`：与 `query_inspect_orders` 同一套聚合写法（`GROUP BY r.order_no`），一行 = 一个售后单，字段：
   `order_no`（售后单号）· `return_no`（退回单号，取 `MIN`）· `return_date` · `turbine_vendor` · `project_site` · `` `lines` ``（明细条数）· `tested`（已检测条数）· `done`（已完结条数）· `handled`（已处理条数）· `registered_at`。
   筛选**完全复用** `_build_where`（关键词 / 精确匹配 / 日期区间 / 待检待处理），所以筛选条件与现在是同一套语义，只是聚合前过滤。
2. `app.py` 新增 `GET /api/returns/orders`（与现有 `/api/returns` **同一份查询参数签名**，参数收集方式照抄 `app.py:834` 的 `locals()` 写法，避免“界面选了却静默丢弃”的老坑）。
3. **右侧明细取数不需要新接口**：`order_no` 已是 `/api/returns` 的精确筛选参数（`core/repository.py:1583`、`app.py:827`），所以右侧直接 `GET /api/returns?order_no=SO…&page_size=500` 即可。

**前端 `static/query.html`**
- 版式改成**左列表 + 右明细**两栏（窄屏自动上下堆叠）：
  - 左：售后单聚合表（上面那 9 列 + 进度），点一行 → 选中高亮 + 右侧加载该单明细。
  - 右：该售后单的**明细记录**（紧凑表，列沿用列设置里勾的列的子集），每行保留现有「查看 / 编辑」入口 → **仍然打开现在那个编辑弹窗**（交互不变）；每行「删除」→ 走新的回收站链路。
- 现有能力**不删**：列设置、关键词搜索、导出（明细级）都保留；右侧面板再加一个「导出本单明细」（`/api/export?order_no=…`，导出接口已支持同一套筛选）。
- 删除入口的归属：左表行提供「删除整个售后单（N 条明细）」，右侧提供明细级勾选 + 批量删除 —— 见下方**待你定**的第 ③ 问。
- 筛选卡：**默认收起**（给 `.card__body` 初始加 `hidden`），按钮文案变「展开筛选 / 收起筛选」，并在有生效筛选时显示「筛选 (N)」提示有多少条条件在用。

---

## 二、回收站模块（新增）

### 表（只新增，不动现有表）
`auth_db.recycle_bin`：

| 列 | 说明 |
| --- | --- |
| `id` | 自增主键 |
| `module` | 模块：`returns` / `inspect` / `handle` / `items` / `delivery` / `ledger` / `auth` |
| `kind` | 动作：`single` / `batch` / `cascade`（级联删除） |
| `title` | 人看的标识：售后单号 / 明细键 / 料号 / 申请单号 / 用户名… |
| `summary` | 一句话摘要：厂家 · 风场 · 件数 · 操作来源 |
| `payload` | **LONGTEXT**：JSON，`{"rows": {"库.表": [整行…]}, "files": []}` —— 被删记录（含级联删除的检测 / 处理记录）的完整行快照，恢复靠它 |
| `row_count` | 涉及行数 |
| `operator` / `source_ip` | 谁删的 |
| `deleted_at` / `restored_at` / `restored_by` | 删除时间 / 恢复时间 / 谁恢复的 |

### 落点：所有“删除记录”的入口统一改走 `core/trash.py`（新）
新增 `core/trash.py`：`snapshot(module, kind, title, summary, tables, operator)` → 写一行；`list_trash(module, keyword, page, page_size)`；`get_trash(id)`；`restore(id, operator)`；`purge(id, operator)`；`purge_expired(days=7)`；`purge_all(operator)`。

挂到下面这些删除动作（**删除行为本身、接口签名与返回体都不变**，只是多写一条回收站记录）：

| 模块 | 删除入口 | 代码位置 |
| --- | --- | --- |
| 返件明细 | 单条删除 / 批量删除（级联检测、处理记录） | `core/repository.py:1480 delete_return`、`:1509 delete_returns` |
| 检测记录 | 单条 / 批量 | `core/repo_inspect.py:136`、`:149` |
| 处理记录 | 单条 / 批量 | `core/repo_handle.py:103`、`:116` |
| 匹配库物料 | 删除物料 | `core/repository.py:507 delete_item` |
| 发货申请 | 删除申请单（含明细 + 关联登记行） | `core/repo_delivery.py:435 delete_request` |
| 发货登记行 | 删除登记 | `core/repo_delivery.py:1436 delete_shipment` |
| 核销台账 | 撤销核销记录（`DELETE /clears/{id}`） | `core/repo_delivery.py:3037 delete_clear` |
| 权限设置 | 删除用户 / 删除权限组 | `core/auth.py:506`、`:389` |

**不进回收站**（不是“记录删除”，写进文档免得以后被追问）：照片二进制文件（`core/photos.py`）、字典候选回收（派生表重建）、ERP 同步整表覆盖、登录会话清理、系统配置项开关。

### 自动清理 + 手动清理
- 自动：复用现成的定时器 `core/sched.py` 里的 `Ticker`（发货明细同步就是它跑的），加一个 `trash_purge` —— 启动后 60 秒首跑、之后**每小时**一次，删 `deleted_at < NOW() - INTERVAL 7 DAY`。保留期默认 **7 天**（常量 `TRASH_KEEP_DAYS = 7`，放 `auth_db.setting` 里可改，页面显示“剩 X 天”）。
- 手动：回收站页面提供 ① 每行「彻底删除」②「清理过期（>7 天）」③「清空回收站」（二次确认，危险按钮）。

### 页面
- 新页 `static/trash.html`：表格（模块 / 标识 / 摘要 / 行数 / 操作人 / 删除时间 / 剩余天数 / 操作），按模块与关键词筛选，行内「详情」（看 payload 里恢复了什么）「恢复」「彻底删除」。
- 菜单：`static/assets/common.js:780 NAV_SECOND` 加一项「回收站」（运维性质，与「自动同步任务」同组）。
- 权限点（沿用 `core/auth.py:44 PERMISSION_GROUPS` / `:110 PAGE_PERMS` 机制）：`page.trash`（看页面）、`act.trash_restore`（恢复）、`act.trash_purge`（彻底删除 / 清空）——默认分配见下方**待你定**的第 ② 问。

### 恢复语义
按 `payload` 逐表 `INSERT` 回原位置；**主键已存在**（例如 `detail_key` 已被重新登记占用）就**拒绝并说明原因**（409 + 人话），不做静默覆盖。

---

## 三、权限组“一表全览”

现状：`static/auth.html:253 groupsView()` 是**卡片**，每个组只显示「N 项权限 · M 人」（`:265`），看不到具体勾了哪些。

方案（**在权限组页签里新增一块只读总览表**，现有卡片与编辑弹窗都保留）：
- 行 = 权限点（按 `PERMISSION_GROUPS` 的三段分组：**界面访问 15 · 数据操作 13 · 系统管理 3 = 31 项**，带中文标签）。
- 列 = 每个权限组（表头显示组名 + `M 人` + 项数；内置管理员组标「锁定」），格子 `✓ / ·`。
- 表尾一行「合计」：每组项数 / 人数。
- 点列头 → 直接打开该组现在的编辑弹窗（编辑流程不变）。
- 页内可切换「按组看（现有卡片） / 一表全览（新表）」，两边数据同源（`GET /api/auth/groups` 的 `perms` 已经带着权限清单，**不用改接口**）。

---

## 四、影响面与验证

**改动文件**：`core/repository.py`、`core/trash.py`（新）、`core/auth.py`、`core/db.py`（新表 DDL）、`core/sched.py`（或 `app.py` 注册 ticker）、`app.py`、`static/query.html`、`static/trash.html`（新）、`static/assets/common.js`、`README.md`、`tests/smoke_test.py`、`tests/check_ui.js`、`tests/check_frontend.js`。

**验证（照旧串行三套件，当前基线 819 / 514 / 86 全绿）**
- 删除 → 回收站 → 恢复 的**真跑往返**（返件明细、物料、发货申请、用户/权限组各一条；并验证主键冲突时拒绝恢复）。
- 7 天过期：造一条 `deleted_at = 8 天前` 的记录 → 跑 `purge_expired` → 只删它，不动 3 天前那条。
- 聚合一致性：左表某售后单的「明细条数」= 右侧明细行数；`/api/returns/orders` 的 `total` 与 `SELECT COUNT(DISTINCT order_no)` 对账。
- 权限矩阵：31 个权限点 × N 个组逐格断言；`page.trash` / `act.trash_restore` / `act.trash_purge` 的越权访问（直接敲 URL、无权限调接口）必须被拒。
- 备份 + 影子库恢复演练（新表要进 dump 与逐表行数比对）。

**不做**：不改现有表结构；不改现有接口签名与返回体；不改现有删除的交互与提示。
