> ⚠️ **历史设计稿**：本文写于 2026-09-22 数据库改 MySQL 之前，
> 文中的 `xxx.db` 即今天的 `xxx_db`（schema），数据模型、关系与字段定义均未变。
> 当前实现与部署方式请以 `README.md` / `deploy/README.md` 为准。

# 发货申请单 · 主表 + 明细字段定义（初版 v0.5）

> 更新日期：2026-09-21 · 范围：**主表（整单信息）+ 明细行（发货内容）**
> （**审批流程不做**，仅预留接口）
> 配套原型：`发货申请单-初版.html`（可直接试填、试校验）
>
> **v0.5 变更**：明细行的主输入改为 **「料号 / 品名 / 规格 / 型号」模糊搜索 → 选候选**，
> **产品型号由选定结果得出**（不再是「先选型号、再挑料号」）。原因见 2.2 ——
> 实测物料库里**料号唯一、型号不唯一**，反过来选必然产生歧义。
> **v0.4 变更**：**「发货内容」由主表单个产品型号改为明细行** —— 同一单可发多种产品。
> 随之：①「是否需要返回」**下移到明细行**（每行独立判断）；② 新增「数量」；
> ③ 型号带出料号 / 品名 / 规格；④「调换原因」仍为整单一个。
> **v0.3 变更**：收件地址支持**整段粘贴「地址 + 电话 + 姓名」并自动拆分**到联系人 / 电话栏。
> **v0.2 变更**：新增「是否需要返回」勾选项、「期望发货日」；系统字段随主表落地并去除审批字段。

---

## 一、主表字段（整单信息 · 8 项，**全部必填**）

整单一次发货的共性信息 —— **填一次，作用于该申请单下的全部明细行**。

带 `*` 者为必填。

| # | 字段标签 | 建议字段名 | 数据类型 / 录入方式 | 校验规则 | 说明 |
|---|---|---|---|---|---|
| 1 | 风机厂家 `*` | `turbine_vendor` | 单选 · 可搜索下拉（候选来自历史值，**允许直接录入新值**） | 必填；≤ 50 字 | 与退回登记「整单信息」的同一字段**共用候选**，也是台账匹配维度之一 |
| 2 | 项目名称 `*` | `project_site` | 单选 · 可搜索下拉（候选来自历史值，允许新增） | 必填；≤ 50 字 | 即退回登记里的「项目风场」，**同名同源**，便于台账按「厂家 + 风场」匹配 |
| 3 | 期望发货日 `*` | `expect_ship_date` | **日期**（日历选择器，`type=date`，值形如 `2026-09-25`） | 必填；格式 `YYYY-MM-DD` | 待发货清单的**排序依据**；早于今日会提示「按立即发货处理」，**不拦提交**（允许补录历史单） |
| 4 | 收件地址 `*` | `ship_address` | **多行文本**（3 行，通栏） | 必填；≤ 200 字 | 要写全：省市区 + 详细地址 + 收件单位。**可整段粘贴「地址 + 姓名 + 电话」**，离开本栏后自动拆分（见第六节） |
| 5 | 联系人 `*` | `ship_contact` | 单行文本 | 必填；≤ 20 字 | 与「电话」同行并排；**可由收件地址自动拆分填入**（人已填过则不覆盖） |
| 6 | 电话 `*` | `ship_phone` | 单行文本 · **联系方式**（`inputmode=tel`，手机端弹数字键盘） | 必填；**格式校验**（见第五节） | 格式不对时单独提示，不只说「必填」；**可由收件地址自动拆分填入** |
| 7 | 调换原因 `*` | `replace_reason` | **多行文本**（4 行，通栏，最宽） | 必填；≤ 500 字；显示字数 | **整单一个**（不随明细行拆分）—— 适合「一批故障件一起换」的常见场景 |
| 8 | 快递要求 `*` | `express_req` | **多行文本**（3 行，通栏） | 必填；≤ 200 字；无特殊要求可写「无」 | 快递公司偏好 / 到付寄付 / 保价 / 包装要求 |

> **为什么「产品型号」不在主表**：v0.3 及以前一单只能填一个型号；实际业务里
> 一张单常混装多种产品（含耗材、附件），所以它下移到明细行（见第二节）。

---

## 二、明细行字段（发货内容 · **一行一种产品**）

| # | 字段标签 | 建议字段名 | 数据类型 / 录入方式 | 校验规则 | 说明 |
|---|---|---|---|---|---|
| 1 | 产品 `*` | `product_search`<br>（**仅前端，不落库**） | **模糊搜索框**：输入 料号 / 品名 / 规格 / 型号 的任意关键词 → 弹出候选 → 选定一项<br>支持空格分词（见 2.3） | 必填（有输入即可） | **明细行的主输入**。它不是落库字段，而是「找到那个物料」的手段 —— 真正落库的是下面 2~5 项 |
| 2 | 料号 | `material_no` | **选定候选后自动带出**（只读） | 不校验 | **物料库的唯一键**（`items.db` 的 `item_master.material_no`，100% 非空且唯一） |
| 3 | 产品型号 | `product_model` | **选定候选后自动带出**（只读） | 不校验 | **由选定结果得出**，不再由人工输入（v0.5 的核心改动）。台账 / 退回登记按型号对齐时要的就是它 |
| 4 | 品名 | `product_name` | 选定后**自动带出**（只读） | 不校验 | 落库时冗余保存（避免日后改名对不上）；**只随选定结果产生**，不取搜索框里敲的文字 |
| 5 | 规格 | `spec` | **自动带出**（只读） | 不校验 | 同「品名」，随选定结果带出 |
| 6 | 数量 `*` | `qty` | 数字（正整数，默认 `1`，步进 1） | 必填；整数且 ≥ 1 | 台账按**数量**核销（而非行数）—— 一行 3 件就是 3 件 |
| 7 | 是否需要返回 | `need_return` | **勾选项**（checkbox，**默认勾选**） | 无需校验（勾 = 需返回） | **台账分流开关（逐行）**：勾选的行进入「未返回」统计，返件到货后按「整机厂家 + 项目风场」自动核销；一次性耗材 / 随机附件可取消勾选 |

### 2.1 为什么「是否需要返回」必须在明细行

同一单里可能**混装** —— 例如：

| 行 | 产品 | 数量 | 需返回？ |
|---|---|---|---|
| 1 | 抗冰冻风向传感器 | 1 | ✅ 要（旧件回收） |
| 2 | 超声波风速风向仪 | 2 | ✅ 要 |
| 3 | 8 芯电缆 | 3 | ❌ 不要（一次性耗材） |

放主表就表达不了这种混装，只能拆成两张申请单。**默认勾选**的理由与 v0.2 相同：
若默认不勾，用户忘了勾会让本该核销的记录**静默漏掉**；忘取消只是多一条**可见**的待核销，
后者可手工清账。两害相权取可见的那种。

### 2.2 为什么主输入是「搜索框」，而不是「型号」

**这是从真实数据里查出来的**：

```
匹配数据库实测（items.db · item_master，1735 条）：
  料号  100% 非空，且唯一
  型号   94% 非空 —— 同一型号常对应多个料号
  例：型号 BLF1-S 对应 100+ 条料号，规格 / 描述各不相同
      型号 BLF1-C 对应 114 条
```

v0.4 的方案是「先选型号 → 再挑料号」，但实测下来这条路要走两步，
而且**第二步（挑哪个料号）用户往往没有依据** —— 同型号各料号的差别在规格和描述里，
从型号本身看不出来。

所以 v0.5 改成 **按 料号 / 品名 / 规格 / 型号 模糊搜索 → 选定一项 → 型号随之得出**：

| 好处 | 说明 |
|---|---|
| **歧义消失** | 料号唯一 → 选定即定，型号不需要人再判断 |
| **记得什么就搜什么** | 记得料号搜料号、只记得「风速传感器」搜品名、记得型号也能搜型号 |
| **判断变简单** | 从「选哪个料号」（用户没依据）变成「从候选里认出哪个物料」（用户本来就要做的事） |

### 2.3 模糊匹配规则

| 步骤 | 规则 |
|---|---|
| ① 归一化 | 全角转半角、去掉所有空白、统一大写。所以 `１０００１－０００１`、`10001 0001`、`blf1-c` 都能搜到 |
| ② 分词 | 按空格切成多个词，**每个词都要命中**（AND）。如「风速 420」= 某字段同时含「风速」与「420」（实测把 525 条收窄到 161 条） |
| ③ 打分 | 料号精确 `1000` > 料号前缀 `800` > 料号包含 `600` > 型号精确 `500` > **型号前缀 `450`** > 型号包含 `400` > 品名 `300` > 规格 `200` > 描述 `100`；多词累加。**料号排最高**因为唯一、命中即定；**型号给了前缀档**因为用户常只记得前几个字符（如 `BLDL03`）；描述最低，是兜底噪声 |
| ④ 排序截断 | 按得分降序、同分按料号升序；**只渲染前 40 项**，超出时提示「继续输入可收窄」 |
| ⑤ 选定 | 点击候选，或 ↑↓ 选择 + Enter 确认；Esc 关闭面板（不改动已填内容） |

**候选每项展示**：料号（等宽字体）· 品名 · 型号 · 规格 · 描述 ——
**描述**字段能区分同型号的不同料号（如「远景、海上型、4芯」），是选对的关键信息。

### 2.4 必须从候选中选定（**不支持手工填写**）

| 情况 | 处理 |
|---|---|
| **有候选但没选** | **拦下**。提交时汇总写 `明细「第 N 行 产品（还没从候选中选定）」`，搜索框保持标黄 |
| **库里没有匹配项** | **拦下**。同上 —— 先请管理员把物料补进库，再回来填 |
| **搜索框为空** | **拦下**。汇总写 `明细「第 N 行 产品」` |

**为什么必须选定**：手填的品名给不出**料号**和**型号** —— 台账核销靠料号、
与退回登记按型号对齐。与其让一条「看得见但没法用」的记录进库，不如在填单时就拦住。

**代价（已知并接受）**：物料库里没有的物料**填不进来**。这是「宁可挡住，也不放脏数据」的取舍；
真遇到紧急调换而库里确实没有，走线下沟通 + 补库。**本版不做**「申请新增物料」的旁路
（见第十节第 14 条）。

### 2.5 人工优先

- 手选的结果**不会被任何自动逻辑改写**；
- **改动搜索文字 = 放弃原选择** —— 料号 / 型号 / 规格 立即清空。
  这条是必须的：否则会出现「看着品名、挂着别的料号」这种最难发现的错配；
- 输入框右侧的 **×** 可一键清除重搜（连搜索文字一起清掉）。


---

## 三、系统字段（随主表一起落地 · **已去除审批字段**）

| 建议字段名 | 字段标签 | 类型 | 生成规则 | 说明 |
|---|---|---|---|---|
| `request_no` | 申请单号 | 文本（唯一） | 提交时生成：`FH` + `YYYYMMDD` + 3 位流水（例 `FH20260921001`） | 对外单号，用于人工沟通与台账引用 |
| `applicant` | 申请人 | 文本 | 取当前登录用户（`users.username`） | 不让用户自己填 —— 避免冒名，也省一个字段 |
| `apply_date` | 申请日期 | 日期 | 提交时的当日日期 | 用于统计与排序；与「期望发货日」区分开 |
| `status` | 状态 | 单选（枚举） | 初始「草稿」；提交后「已提交」（进待发货清单）→「已发货」→「已关闭」 | **不含审批环节**：提交即进入待发货队列 |
| `line_no` | 行号 | 整数 | 明细行按顺序生成 1、2、3… | 明细子表用；与主表构成「一行一产品」，台账按此逐行核销 |

**审批相关字段（审批人 / 审批时间 / 审批意见 / 驳回原因）本版不建字段、不建表** —— 见第四节的预留方案。

---

## 四、状态机与审批预留接口

### 4.1 当前状态机（无审批）

```
草稿(draft) ──提交──▶ 已提交(submitted) ──发货确认──▶ 已发货(shipped)
                          │  ▲                              │
                          │  └──── 退回/终止 ────┐          │
                          └──────────────────▶ 已关闭(closed) ◀──┘
```

| 取值 | 中文 | 可达性 | 进入方式 |
|---|---|---|---|
| `draft` | 草稿 | 可达 | 暂存未提交（下一版做「暂存」按钮） |
| `submitted` | 已提交 | 可达 | **提交后立即进入**，同时进入「待发货清单」 |
| `shipped` | 已发货 | 可达 | 发货跟踪从 ERP 同步到实际发货数据后回写 |
| `closed` | 已关闭 | 可达 | 整单完成或人工终止 |

> 状态挂在**主表**上（整单一个状态），明细行不单独设状态 ——
> 一单要么整单待发、要么整单已发。

### 4.2 审批预留（**不实现，只留扩展点**）

将来要接审批流程，**只需三处改动，业务代码（待发货清单、发货跟踪、核销台账）不用动**：

```python
# ---------- ① 状态机启用两个预留取值 ----------
DELIVERY_STATUS = {
    "draft":            "草稿",
    "submitted":        "已提交",   # 无审批时：提交后直接到此
    "shipped":          "已发货",
    "closed":           "已关闭",
    # ---- 以下为审批流程预留，当前不可达 ----
    # "pending_approval": "待审批",
    # "rejected":         "已驳回",
}

# ---------- ② 三个状态流转钩子（当前全为空实现）----------
# 接审批时的改法：
#   on_submit  → 状态改判到 pending_approval（进审批队列，而不是直接进待发货清单）
#   on_approve → 状态改判到 submitted（此时才进待发货清单）
#   on_reject  → 状态改判到 rejected（回申请人修改）
# 下游只认「状态是否 = submitted」，所以不必改动它们。
DELIVERY_HOOKS = {
    "on_submit":  None,   # None = 无审批，提交即 submitted
    "on_approve": None,   # 预留
    "on_reject":  None,   # 预留
}

# ---------- ③ 将来新建 delivery_audit 表（记录审批动作）----------
# 与 delivery_request 1:N；字段：request_no · action · operator · acted_at · comment
```

**为什么这样预留**：判断「能否发货」的唯一依据是 `status == 'submitted'`。
把「怎么变成 submitted」抽成钩子后，加审批只是**换个进入方式**，
而不是在每处业务代码里插判断 —— 这是这次预留的全部价值。

---

## 五、校验规则

| 项 | 规则 | 触及时的提示 |
|---|---|---|
| **主表必填（8 项）** | 去掉首尾空格后为空即不通过 | 该字段下方红字：`请填写「×××」` + 输入框描红（日期字段为 `请选择「期望发货日」`） |
| **明细必填（逐行）** | **产品必须已从候选中选定**（手填不算）；数量为空 / 非数字 / < 1 / 非整数 均不通过 | 明细行对应的格子**描红**；顶部汇总写 `明细 「第 N 行 产品（还没从候选中选定）」`（带行号 + 原因） |
| **搜索框三态** | 空 / 有输入未选定 / 已选定 | 未选定**黄**边黄底（提示"待选定"），选定后**绿**边绿底。三态集中在一处计算，避免出现「选了却还标黄」 |
| **未选定 / 库外物料** | **拦截提交**（见 2.4）—— 产品必须已从候选中选定（`picked = true`），手填不算 | ① 表格下方黄字提示（区分「有候选但还没选定」与「库中无匹配项」）；② 提交汇总写 `明细「第 N 行 产品（还没从候选中选定）」` |
| **明细至少一行** | 明细表始终保留 ≥ 1 行 | 只有一行时该行的删除按钮**禁用**（避免删成空表） |
| **电话格式** | 手机 `^1[3-9]\d{9}$`；座机 `^0\d{2,3}-?\d{7,8}(-\d{1,5})?$`（支持分机） | `格式不对：手机填 11 位（1 开头）；座机写「区号-号码」，如 0577-12345678` |
| **日期格式** | `^\d{4}-\d{2}-\d{2}$`（由 `type=date` 保证，兜底再校验一次） | `日期格式应为 YYYY-MM-DD，如 2026-09-21` |
| **日期合理性** | 早于今日 → **提示但不拦截** | 控件下方黄字：`已早于今天，将按「立即发货」处理` |
| **勾选项** | **不参与必填校验**（没有「空值」概念） | 无错误态 |
| **长度上限** | 短文本 ≤ 50 / 地址 ≤ 200 / 调换原因 ≤ 500，超出无法继续输入 | 输入框右下角实时显示 `已填/上限` 字数 |
| **提交时汇总** | 一次列出全部未通过项 | 表单顶部红条：`还有 N 项需要修正：「×××」、明细 「第 1 行 数量」。带 * 的是必填项，请补齐后再提交。` |
| **焦点定位** | 提交被拦下时 | 光标停在**第一个**未通过处（主表字段，或明细首行的**产品搜索框**） |
| **边改边消错** | 用户开始修改某个已报错的字段 | 该字段 / 该格的描红**即时消失** |

---

## 六、收件地址的自动拆分（v0.3 新增）

### 6.1 它解决什么

填单时地址、电话、姓名往往是一整段贴着（从聊天记录 / 面单复制的常见形态），
顺序还不固定。以前要手工切成三段、分别填三个框；现在**整段粘进收件地址即可**：

| 粘贴的内容 | 拆分后 |
|---|---|
| `浙江省温州市乐清市经济开发区纬十二路228号  夏鹏程  18815120633` | 地址栏 `浙江省温州市乐清市经济开发区纬十二路228号` · 联系人 `夏鹏程` · 电话 `18815120633` |
| `河北省承德市宽城县民族街729号上海电气项目部  15320324354  周杰` | 地址栏 `河北省承德市宽城县民族街729号上海电气项目部` · 电话 `15320324354` · 联系人 `周杰` |

上面两种顺序（**姓名在前 / 电话在前**）结果完全相同。
顺带也支持「姓名 电话 地址」「电话 姓名 地址」两种排列。

### 6.2 识别规则

| 步骤 | 规则 |
|---|---|
| ① 找电话 | 手机 `1[3-9]` 开头 11 位（**允许中间夹空格 / 短横**，如 `153 2032 4354` → 规范化成连写）；或座机「区号-号码(分机)」。前后各用「非数字」边界卡住，避免从长数字串里截出一段 |
| ② 找姓名 | 先看电话**后面**紧跟的一段，不行再看电话**前面**最后一段。必须是 **2~4 个汉字**，且**不含地址特征字**（省 / 市 / 县 / 区 / 街 / 路 / 号 / 栋 / 楼 / 室 / 司 / 部 / 厂 / 场 / 站 / 城 / 庄 …） |
| ③ 剩下的是地址 | 把电话与姓名**按位置**切掉，空白折叠成一个空格，首尾去空 |

### 6.3 两条安全底线（都是为了让「猜错」不至于静默损坏数据）

1. **找不到电话就完全不动。** 宁可什么都不做，也不去猜。地址一字不改（连多余空格都不折叠）。
2. **姓名必须紧挨着电话、且不含地址特征字。** 方向是「宁可漏判」——
   漏判只是没自动填（用户手打一下），错判会把地址片段（如 `宽城`）吞进联系人栏、
   让地址凭空少一截，而且很难发现。

### 6.4 人工优先

遵循项目铁律「自动填的值要能被人为覆盖，且后续自动逻辑不得改写人工值」：

- 实现方式是**记录上次自动填入的值**（`autoVals`）。只有「当前值 === 上次自动填入的值」
  （说明人没动过）才允许再次改写；
- **人一改过就永久让位** —— 比如先手填了联系人「张三」，之后再怎么拆都不会覆盖它；
- 被挡下的那一栏会在提示里**明确说明**「未覆盖」，不会静默跳过。

### 6.5 什么时候触发

| 触发点 | 说明 |
|---|---|
| **离开地址栏（blur）** | 主要时机 —— 你填完切走 |
| **粘贴（paste）后** | 整段粘进来即拆，不用再点一下 |
| **手动按钮** | 地址下方有「从地址拆分电话 / 姓名」，可随时重试；**失败时会说明原因**（如「没找到电话」） |

> **刻意不在每次输入时触发** —— 打字打到一半、地址还没写完，会被误拆。
> 拆分结果会在地址下方用一行小字汇报（绿字成功 / 黄字提示），你改地址时这行会收掉。

---

## 七、布局与排列顺序

**排列动线**：`谁的 → 发什么 → 发给谁 → 为什么 / 怎么发`

| 段 | 内容 | 布局 |
|---|---|---|
| ① 业务归属 | 风机厂家 → 项目名称 | 两列并排 |
| ② 发货明细 | **整张明细表**（一行一种产品） | 通栏表格，列序：**产品（搜索）** → 数量 → 需返回 → 料号 / 型号 / 规格 |
| ③ 发货安排与收件 | 期望发货日 → 收件地址 → 联系人 + 电话 | 日期单行；地址通栏（3 行）；联系人/电话两列并排 |
| ④ 原因与要求 | 调换原因 → 快递要求 | 通栏（4 行 / 3 行） |

**为什么这么排**：
1. 厂家与项目放最前 —— 它们决定后续台账按「厂家 + 风场」匹配，先把归属定下来；
2. **明细表放第二段** —— 它是这张单的**核心**，填单的人第一件事就是确认发什么；
   表内列序＝录入动线：**产品搜索（主输入）→ 数量 → 需返回**，右侧三列（料号 / 型号 / 规格）
   是选定后自动带出的只读结果，靠右放，不打断录入节奏；
   品名不单独占列 —— 它显示在搜索框里（选定后框内即为品名），避免表格过宽；
3. 「什么时候发」与「发到哪儿、给谁」都是**整单共用**的信息，聚在第三段便于核对；
4. 两个**长文本放最后**，避免多行输入打断前面的节奏；
5. **联系人与电话同排** —— 同一件事的两个属性，并排便于核对。

**明细表的交互与视觉**：
- **末行任意格回车 = 新增一行**（焦点停在新行同一格）；中间行回车 → 焦点下移一格；
- 格子**默认无边框**（悬停 / 聚焦时才现边框），让表格看起来像一张纸而不是一堆输入框；
- 只读列（品名 / 规格）用次要色，与可编辑列区分；
- **候选面板挂在 `<body>` 上用 `position:fixed` 定位** —— 明细表容器有 `overflow-x:auto`，
  用 `absolute` 挂在单元格里会被裁掉；下方空间不足时自动翻到输入框**上方**；
- 候选面板支持 **↑↓ 选择 · Enter 确认 · Esc 关闭**；打开面板时 Enter **不再触发「新增一行」**
  （否则「按回车选定」会变成「新增一行」）；
- 已选定的行**再聚焦不弹面板**（不打扰）；输入框右侧 **×** 可一键清除重搜；
- 表下方有「＋ 新增一行」/「清空明细」与**实时统计**
  （`共 N 行（已填 M 行）· 合计 X 件 · 需返回 Y 件` + 未选定行数）。
  统计**只算「产品搜索框有输入」的行** —— 空白行不该被算成 1 件（否则刚打开页面就显示「合计 1 件」，很反直觉）。

**通用对齐与控件**：
- 标签统一在左、固定 92px 且右对齐；控件左边界对齐；错误提示与字数统计固定在控件下方（不跳动）。
- 勾选项**不做描红**（无「空值」概念）；
- 日期框限宽 190px（日期本身很短，拉满会显得空），下方提示随所选日期变化；
- 表单顶部有一行**只读系统字段条**（申请单号 / 申请人 / 申请日期 / 状态）。

> 窄屏（< 720px）：主表降为「标签在上、控件在下」的单列；明细表**横向滚动**。

---

## 八、可直接落库的定义

```python
# ---------- 主表（整单信息）----------
DELIVERY_FIELDS = [
    {"name": "turbine_vendor",   "label": "风机厂家", "type": "search-dict",
     "source": "dict",      "required": True, "max": 50,
     "placeholder": "如 金风科技（可选已有值，也可直接录入）"},
    {"name": "project_site",     "label": "项目名称", "type": "search-dict",
     "source": "history",   "required": True, "max": 50,
     "placeholder": "与退回登记的「项目风场」同源"},
    {"name": "expect_ship_date", "label": "期望发货日", "type": "date",
     "required": True},
    {"name": "ship_address",     "label": "收件地址", "type": "textarea", "rows": 3,
     "required": True, "max": 200,
     "split_targets": ["ship_contact", "ship_phone"]},   # 可整段粘贴自动拆分
    {"name": "ship_contact",     "label": "联系人",   "type": "text",
     "required": True, "max": 20, "auto_fillable": True},
    {"name": "ship_phone",       "label": "电话",     "type": "text",
     "inputmode": "tel",    "required": True, "max": 20, "auto_fillable": True,
     "pattern": r"^(1[3-9]\d{9}|0\d{2,3}-?\d{7,8}(-\d{1,5})?)$"},
    {"name": "replace_reason",   "label": "调换原因", "type": "textarea", "rows": 4,
     "required": True, "max": 500},
    {"name": "express_req",      "label": "快递要求", "type": "textarea", "rows": 3,
     "required": True, "max": 200},
]

# ---------- 明细行（发货内容 · 一行一种产品）----------
DELIVERY_ITEM_FIELDS = [
    {"name": "product_search", "label": "产品", "type": "search-item",
     "source": "items", "required": True,
     "placeholder": "料号 / 品名 / 规格 / 型号",
     # 参与匹配的字段与权重（高 → 低）：料号 > 型号 > 品名 > 规格 > 描述
     "match_fields": ["material_no", "product_model", "product_name", "spec",
                      "description"],
     "match_mode": "and-words",     # 空格分词 = 每词都要命中
     "match_limit": 40,             # 候选最多渲染 40 项，超出提示「继续输入可收窄」
     "normalize": ["fullwidth", "strip-space", "upper"],
     "on_empty": "reject",          # 搜不到 / 没选定 → 校验不通过（产品必须从候选中选定）
     "derives": ["material_no", "product_model", "product_name", "spec"]},
    {"name": "qty",           "label": "数量", "type": "number",
     "required": True, "min": 1, "step": 1, "default": 1},
    # 逐行决定，默认勾选（理由见 2.1）
    {"name": "need_return",   "label": "是否需要返回", "type": "checkbox",
     "default": True,
     "hint_true":  "需返回：进入「未返回」统计，返件到货后自动核销",
     "hint_false": "不需返回：不进「未返回」统计（一次性耗材等）"},
    {"name": "material_no",   "label": "料号",     "type": "text", "readonly": True},
    {"name": "product_model", "label": "产品型号", "type": "text", "readonly": True},
    {"name": "product_name",  "label": "品名",     "type": "text", "readonly": True,
     "from": "material_no"},          # 只随选定结果产生，不取输入文字
    {"name": "spec",          "label": "规格",     "type": "text", "readonly": True},
]

# ---------- 系统字段 ----------
DELIVERY_SYS_FIELDS = [
    {"name": "request_no", "label": "申请单号", "type": "text",   "auto": "submit"},
    {"name": "applicant",  "label": "申请人",   "type": "text",   "auto": "session"},
    {"name": "apply_date", "label": "申请日期", "type": "date",   "auto": "today"},
    {"name": "status",     "label": "状态",     "type": "select", "auto": "draft",
     "options": ["draft", "submitted", "shipped", "closed"]},
    {"name": "line_no",    "label": "行号",     "type": "int",    "auto": "sequence"},
    # 审批字段（approver / approved_at / approve_comment）本版不建 —— 见第四节
]

# ---------- 表单分组（决定渲染顺序与布局）----------
DELIVERY_GROUPS = [
    {"title": "业务归属",       "fields": ["turbine_vendor", "project_site"]},
    {"title": "发货明细",       "fields": ["__items__"]},        # 明细表占位
    {"title": "发货安排与收件", "fields": ["expect_ship_date", "ship_address",
                                          "ship_contact", "ship_phone"]},
    {"title": "原因与要求",     "fields": ["replace_reason", "express_req"]},
]
```

### 建议的建表片段（`delivery.db` · 主表 + 明细表）

```sql
-- ============ 主表：整单信息（v0.4 起 产品型号 / need_return 已下移到明细表）============
CREATE TABLE IF NOT EXISTS delivery_request (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,

    -- ---- 主表填写字段（8 项）----
    turbine_vendor   TEXT NOT NULL,
    project_site     TEXT NOT NULL,
    expect_ship_date TEXT NOT NULL,                -- YYYY-MM-DD
    ship_address     TEXT NOT NULL,
    ship_contact     TEXT NOT NULL,
    ship_phone       TEXT NOT NULL,
    replace_reason   TEXT NOT NULL,
    express_req      TEXT NOT NULL,

    -- ---- 系统字段 ----
    request_no       TEXT UNIQUE,                  -- FH + YYYYMMDD + 3 位流水
    applicant        TEXT,                         -- 登录用户
    apply_date       TEXT,                         -- YYYY-MM-DD
    status           TEXT NOT NULL DEFAULT 'draft',-- draft/submitted/shipped/closed

    created_at       TEXT,
    updated_at       TEXT
);
CREATE INDEX IF NOT EXISTS idx_dreq_status ON delivery_request(status);
CREATE INDEX IF NOT EXISTS idx_dreq_expect ON delivery_request(expect_ship_date);
-- 台账核销要按「整机厂家 + 项目风场」匹配，这两列值得建联合索引
CREATE INDEX IF NOT EXISTS idx_dreq_match  ON delivery_request(turbine_vendor, project_site);

-- ============ 明细表：发货内容（一行一种产品）============
CREATE TABLE IF NOT EXISTS delivery_request_item (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    request_no     TEXT NOT NULL,                  -- 关联主表的对外单号（便于对账）
    line_no        INTEGER NOT NULL,               -- 1、2、3…

    -- 以下四列由「产品搜索」选定候选后一次性带出（product_search 本身不落库）
    material_no    TEXT,                           -- 料号（物料库唯一键，核销主依据）
    product_model  TEXT NOT NULL,                  -- 型号（由选定结果得出）
    qty            INTEGER NOT NULL DEFAULT 1,     -- 数量（台账按数量核销）
    need_return    INTEGER NOT NULL DEFAULT 1,     -- 1=需返回，0=不需返回（逐行）

    -- 由选定结果从物料库带出的冗余字段：落库保存，避免日后改名对不上
    product_name   TEXT,                           -- 库外物料时 = 申请人输入的文字
    spec           TEXT,

    created_at     TEXT,
    updated_at     TEXT,
    UNIQUE (request_no, line_no)                   -- 同一单内行号唯一
);
CREATE INDEX IF NOT EXISTS idx_ditem_req      ON delivery_request_item(request_no);
CREATE INDEX IF NOT EXISTS idx_ditem_material ON delivery_request_item(material_no);
-- 台账按「厂家 + 风场 + 料号」核销时，这个联合索引能少一次回表
CREATE INDEX IF NOT EXISTS idx_ditem_match    ON delivery_request_item(need_return, material_no);
```

**落点**：`delivery.db` 的 `delivery_request`（主表）+ `delivery_request_item`（明细），
一个模块一个库，见《框架大纲》5.5。

---

## 九、与既有系统的衔接

| 衔接点 | 说明 |
|---|---|
| 风机厂家 / 项目名称 | 与退回登记整单信息的 `turbine_vendor` / `project_site` **同名同义**，共用候选字典 —— 这是核销台账按「厂家 + 风场」核销的前提 |
| 物料搜索 | 复用匹配数据库（`items.db` 的 `item_master`），与退回登记的料号回填**同一套数据、同一方向**（料号/品名/规格 → 型号）。差别只在精度：退回登记扫条码（精确命中），这里允许模糊搜（人工从候选里认） |
| **⚠️ 核销粒度差异** | 退回登记**一行 = 一件产品**（`detail_key` 一件一个键）；发货明细**一行 = 一种产品 + 数量**（可能 N 件）。所以台账核销是 **「1 行发货 ↔ N 条返件」**，不能靠行号直接对应，必须按**数量**匹配 —— 这是实现台账时最容易想错的地方 |
| `need_return` 与核销台账 | 「剩余未返回」只统计 `need_return = 1` 的行，且按 **`qty` 累加件数**（不是行数）；`= 0` 的行只走发货流程 |
| `status` 与待发货清单 | 待发货清单 = `status = 'submitted'` 的派生视图（**不落表**，见《框架大纲》5.2 ②） |
| 地址拆分 | 纯前端行为，不涉及落库；拆分结果仍是 `ship_address` / `ship_contact` / `ship_phone` 三个独立字段，后端无感知 |
| 字段风格 | 与既有字段定义一致（`type` / `source` / `required` / `max`），可直接被现有渲染逻辑消费 |
| 权限 | 新增 `page.delivery_apply` 与 `act.delivery_submit`（见《框架大纲》5.6） |

---

## 十、待确认 / 下一版可迭代项

| # | 事项 | 现状 / 建议 |
|---|---|---|
| 1 | ~~「是否需要返回」开关~~ | ✅ v0.2 加入，**v0.4 下移到明细行**（逐行判断） |
| 2 | ~~是否要「期望发货日」~~ | ✅ v0.2 已加入（日期，必填，待发货清单排序依据） |
| 3 | ~~型号 vs 料号：谁做主输入？~~ | ✅ **v0.5 已改**：主输入改为「料号 / 品名 / 规格 / 型号」**模糊搜索 → 选候选**，型号由选定结果得出 —— 型号不唯一的歧义因此消失 |
| 4 | **明细行要不要「备注」** | 当前无。若某些行常有特殊说明（如「含安装支架」），可加一列 |
| 5 | **回车换行 / Excel 粘贴** | 已做「末行回车新增一行」；**Excel 多行粘贴**（参照退回登记页）未做，建议下一版补 |
| 6 | **系统字段是否在页面显示** | 现为表单顶部一行只读条。不想显示的话去掉 `.sysbar` 即可 |
| 7 | **单号流水号规则** | 现为 `FH+YYYYMMDD+3位`（当日递增）。若要跨日连续（`FH000123`）请说明 |
| 8 | **「期望发货日」是否允许早于今日** | 现为**提示不拦截**（允许补录历史单）。要硬拦（必须 ≥ 今日）请说明 |
| 9 | **字段名确认** | `project_site`（对齐退回登记，**当前采用**）还是 `project_name`（对齐「项目名称」字面） |
| 10 | **拆分的触发时机 / 提示** | 现为「失焦 + 粘贴 + 手动按钮」，且成功时给一行小字。嫌粘贴太主动或提示啰嗦都可调 |
| 11 | **候选上限 40 条** | 常见词命中很多（实测「风速」525 条、「10001」114 条），只渲染前 40 条并提示收窄。嫌少 / 嫌多可调 `MATCH_LIMIT` |
| 12 | **搜索是否要按客户 / 类别过滤** | 候选里已展示「描述」（常含客户信息，如「远景、海上型、4芯」），但没单独做「按客户筛」。若常按客户找物料，可加一个客户下拉过滤 |
| 13 | **要不要记住常用物料** | 若某些料号反复发（如标准备件），可在空态时列出「最近 / 最常发的物料」快捷入口。需要的话下一版加 |
| 14 | **库外物料填不进来（v0.6 收紧的代价）** | 产品必须从候选中选定后，**物料库里没有的物料就填不了单**。当前按要求从严（宁可挡住，也不放脏数据）。若实际常遇到「新品 / 库中漏录」，建议下一版补一个旁路：允许带 `is_off_catalog` 标记 + **强制手填型号**（料号留空），或做一个「申请新增物料」的轻流程 |
